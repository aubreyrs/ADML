[package]
name = "adml"
version = "0.1.0"
edition = "2021"

[dependencies]
colored = "2.1" # for colored terminal output
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dependencies.uuid]
version = "1.10.0"
features = [
    "v4"
]